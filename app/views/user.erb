<h2><%= @user.name %></h2>
<% @tweets.each do |tweet| %>
  <p><%= tweet.status %></p>
<% end %>

<% if current_user.id == @user.id %>
  <h3>Add a tweet</h3>
  <form action="/tweets" method="POST">
    <p><input type="textarea" name="status"></p>
    <input type="hidden" name="user_id" value="<%= @user.id %>">
    <input class="btn btn-primary" type="submit">
  </form>
<% end %>